# properties/conf file location. add additional locations by prefixing with "config_"
# location from root directory ( where pom.xml file is located at )

config.api = "../apiTestData/"
config.ui = ./resources/properties